import { googleApis } from '../config';

export async function getSearchConsoleData(auth: any, siteUrl: string) {
  // This function is a placeholder and will be implemented later
  console.log('Fetching Search Console data...');
  return [];
}

export async function getGoogleAdsData(auth: any, customerId: string) {
  // This function is a placeholder and will be implemented later
  console.log('Fetching Google Ads data...');
  return [];
}